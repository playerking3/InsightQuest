import {Text, View} from "react-native";
import {css} from "../style/HeaderStyle";

export default function (){
    return(
        <View style={css.fundo}>
            <Text style={css.texto}>Miopia</Text>
        </View>
    )
}